---
phase: 02-permission-ux
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - components/PermissionRecovery.tsx
  - components/PresentationView.tsx
autonomous: true

must_haves:
  truths:
    - "When permission denied, teacher sees inline recovery text with help link"
    - "Help link opens modal with browser-specific reset instructions"
    - "Modal detects Chrome vs Edge and shows appropriate steps"
    - "Modal can be closed, returns teacher to presentation"
  artifacts:
    - path: "components/PermissionRecovery.tsx"
      provides: "Recovery modal with browser detection"
      exports: ["PermissionRecovery"]
      min_lines: 80
    - path: "components/PresentationView.tsx"
      provides: "Inline recovery text integration"
      contains: "Permission denied"
  key_links:
    - from: "components/PresentationView.tsx"
      to: "components/PermissionRecovery.tsx"
      via: "import and conditional render"
      pattern: "import.*PermissionRecovery"
---

<objective>
Create recovery UI for when permission has been denied, showing browser-specific reset instructions.

Purpose: Teacher who previously denied permission knows exactly how to fix it. Inline text points to help, modal provides step-by-step browser-specific guidance.

Output: New PermissionRecovery.tsx component with modal, integrated into PresentationView.tsx with inline trigger.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-permission-ux/02-CONTEXT.md
@.planning/phases/02-permission-ux/02-RESEARCH.md
@.planning/phases/02-permission-ux/02-02-SUMMARY.md

@components/PresentationView.tsx
@components/ManualPlacementGuide.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PermissionRecovery component</name>
  <files>components/PermissionRecovery.tsx</files>
  <action>
Create a new component that displays browser-specific permission reset instructions in a modal:

```tsx
import React, { useState } from 'react';

type BrowserType = 'chrome' | 'edge' | 'unknown';

/**
 * Detects browser type for permission reset instructions.
 * Edge UA includes "Chrome" so must check Edg/ first.
 */
const detectBrowser = (): BrowserType => {
  const ua = navigator.userAgent;
  if (ua.includes('Edg/')) return 'edge';
  if (ua.includes('Chrome/') && !ua.includes('Chromium/')) return 'chrome';
  return 'unknown';
};

const browserInstructions: Record<BrowserType, { name: string; steps: string[] }> = {
  chrome: {
    name: 'Chrome',
    steps: [
      'Click the lock icon (or tune icon) in the address bar',
      'Select "Site settings"',
      'Find "Window management" under Permissions',
      'Change from "Block" to "Ask" or "Allow"',
      'Refresh this page'
    ]
  },
  edge: {
    name: 'Edge',
    steps: [
      'Click the lock icon in the address bar',
      'Select "Permissions for this site"',
      'Find "Window management"',
      'Change from "Block" to "Ask" or "Allow"',
      'Refresh this page'
    ]
  },
  unknown: {
    name: 'your browser',
    steps: [
      'Open your browser settings',
      'Find Site permissions or Site settings',
      'Look for Window management',
      'Reset or allow permission for this site',
      'Refresh this page'
    ]
  }
};

interface PermissionRecoveryProps {
  onClose: () => void;
}

/**
 * Modal showing browser-specific instructions to reset window management permission.
 * Displayed when teacher clicks "Learn how" link after permission was denied.
 */
const PermissionRecovery: React.FC<PermissionRecoveryProps> = ({ onClose }) => {
  const browser = detectBrowser();
  const instructions = browserInstructions[browser];

  return (
    <div className="fixed inset-0 z-[100] bg-black/50 backdrop-blur-sm flex items-center justify-center p-4 animate-fade-in font-poppins">
      <div className="bg-white dark:bg-slate-800 rounded-2xl p-6 max-w-md w-full shadow-2xl">
        {/* Header */}
        <div className="flex items-start justify-between mb-4">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 bg-amber-100 dark:bg-amber-900/30 rounded-full flex items-center justify-center">
              <svg className="w-5 h-5 text-amber-600 dark:text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>
            <div>
              <h2 className="font-bold text-slate-800 dark:text-white text-lg">
                Reset Permission
              </h2>
              <p className="text-sm text-slate-500 dark:text-slate-400">
                for {instructions.name}
              </p>
            </div>
          </div>
          <button
            onClick={onClose}
            className="p-1.5 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors"
          >
            <svg className="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        {/* Instructions */}
        <div className="bg-slate-50 dark:bg-slate-700/50 rounded-xl p-4 mb-4">
          <p className="text-sm text-slate-600 dark:text-slate-300 mb-3">
            To enable auto-placement on your projector:
          </p>
          <ol className="space-y-2">
            {instructions.steps.map((step, index) => (
              <li key={index} className="flex items-start gap-3 text-sm">
                <span className="w-5 h-5 bg-blue-500 text-white rounded-full flex items-center justify-center text-xs font-bold shrink-0">
                  {index + 1}
                </span>
                <span className="text-slate-700 dark:text-slate-200">{step}</span>
              </li>
            ))}
          </ol>
        </div>

        {/* Footer */}
        <div className="flex justify-between items-center">
          <p className="text-xs text-slate-400 dark:text-slate-500">
            Manual dragging still works if you prefer
          </p>
          <button
            onClick={onClose}
            className="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-lg transition-colors"
          >
            Got it
          </button>
        </div>
      </div>
    </div>
  );
};

export default PermissionRecovery;
```

Key features:
- Browser detection (Chrome vs Edge vs unknown)
- Numbered step-by-step instructions
- Modal overlay with backdrop blur
- Dark mode support
- Close button and "Got it" button both dismiss
  </action>
  <verify>
Run: `npx tsc --noEmit`
Read the file and verify all browser instructions are present.
Verify browser detection checks Edg/ before Chrome/.
  </verify>
  <done>PermissionRecovery.tsx created with browser-specific instructions</done>
</task>

<task type="auto">
  <name>Task 2: Integrate inline recovery text and modal</name>
  <files>components/PresentationView.tsx</files>
  <action>
Add inline recovery text and modal trigger when permission is denied:

1. Import the new component:
   ```tsx
   import PermissionRecovery from './PermissionRecovery';
   ```

2. Add state for modal visibility (near other state declarations):
   ```tsx
   const [showRecoveryModal, setShowRecoveryModal] = useState(false);
   ```

3. Add inline recovery text near the launch button (after the permission link from Plan 02):
   ```tsx
   {/* Recovery text - only when denied state and multi-screen */}
   {!isLoading && permissionState === 'denied' && hasMultipleScreens && !isConnected && (
     <span className="text-xs text-slate-400 ml-2">
       Permission denied.{' '}
       <button
         onClick={() => setShowRecoveryModal(true)}
         className="text-amber-400 hover:text-amber-300 underline"
       >
         Learn how to reset
       </button>
     </span>
   )}
   ```

4. Add modal render (at the end of the component, before final closing div):
   ```tsx
   {/* Permission Recovery Modal */}
   {showRecoveryModal && (
     <PermissionRecovery onClose={() => setShowRecoveryModal(false)} />
   )}
   ```

5. The inline text should appear in the header bar near the launch button, styled subtly.

6. Keep ManualPlacementGuide as-is - it handles the "drag to projector" instructions for non-Chromium browsers. The recovery modal is specifically for "you denied permission, here's how to fix it" on Chromium.
  </action>
  <verify>
Run: `npx tsc --noEmit`
Read the modified code. Verify:
- PermissionRecovery imported
- showRecoveryModal state exists
- Inline text appears when permissionState === 'denied'
- "Learn how to reset" link opens modal
- Modal renders when showRecoveryModal is true
- ManualPlacementGuide still renders for !isSupported case
  </verify>
  <done>Recovery text and modal integrated into PresentationView</done>
</task>

</tasks>

<verification>
- [ ] TypeScript compiles: `npx tsc --noEmit`
- [ ] PermissionRecovery.tsx exists with browser detection logic
- [ ] Browser detection checks Edg/ before Chrome/ (avoid misdetection)
- [ ] Inline "Permission denied. Learn how to reset" appears when denied (inspect code)
- [ ] Modal opens when clicking "Learn how to reset" (inspect code)
- [ ] Modal shows Chrome-specific steps for Chrome, Edge-specific for Edge
- [ ] Modal can be closed via X button or "Got it" button
- [ ] ManualPlacementGuide still shows for Firefox/Safari (not broken)
</verification>

<success_criteria>
1. PERM-06 satisfied: Recovery UI shown when permission previously denied
2. Browser-specific guidance helps teacher find exact settings location
3. Non-intrusive: inline text + modal, not blocking UI
4. Both browsers supported: Chrome and Edge (main Chromium variants)
</success_criteria>

<output>
After completion, create `.planning/phases/02-permission-ux/02-03-SUMMARY.md`
</output>
