---
phase: 07-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/logo.png
  - public/favicon.png
  - index.html
  - App.tsx
  - components/ResourceHub.tsx
autonomous: false

must_haves:
  truths:
    - "Header displays PiPi logo image instead of 'L' icon and 'LessonLens' text"
    - "Browser tab shows 'PiPi' as page title"
    - "Browser tab shows PiPi favicon"
    - "ResourceHub preview footer shows PiPi logo instead of 'LessonLens' text"
    - "ResourceHub print output shows 'Created with PiPi' watermark"
  artifacts:
    - path: "public/logo.png"
      provides: "PiPi logo image"
    - path: "public/favicon.png"
      provides: "Browser tab favicon"
    - path: "index.html"
      provides: "Page title and favicon link"
      contains: "<title>PiPi</title>"
    - path: "App.tsx"
      provides: "Header with logo image"
      contains: "logo.png"
    - path: "components/ResourceHub.tsx"
      provides: "Watermarks with PiPi branding"
      contains: "PiPi"
  key_links:
    - from: "index.html"
      to: "public/favicon.png"
      via: "link rel=icon"
      pattern: "href=./PiPi/favicon.png"
    - from: "App.tsx"
      to: "public/logo.png"
      via: "img src"
      pattern: "src=./PiPi/logo.png"
---

<objective>
Replace LessonLens branding with PiPi identity throughout the application.

Purpose: Complete the v2.1 milestone by giving the app its final brand identity. Users will see "PiPi" branding consistently across header, browser tab, and generated resources.

Output: Updated index.html, App.tsx, ResourceHub.tsx with PiPi branding; new public/ directory with logo.png and favicon.png assets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-branding/07-CONTEXT.md
@.planning/phases/07-branding/07-RESEARCH.md

Key implementation details from CONTEXT.md:
- Logo only in header (no "PiPi" text beside it)
- Logo size matches current icon height (~24-32px)
- Logo is not clickable (purely visual)
- ResourceHub watermark uses small logo image
- Favicon derived from main logo

Key technical details from RESEARCH.md:
- Project has `base: '/PiPi/'` in vite.config.ts - asset paths must include this
- Use CSS `mix-blend-mode: multiply` for JPEG white background removal
- Modern favicon: single 48x48 PNG, no ICO needed
- Test dark mode - may need different blend mode or PNG conversion

Current branding locations:
- index.html line 7: `<title>LessonLens - AI Presentation Creator</title>`
- App.tsx lines 524-527: "L" icon div + "LessonLens" h1 in header
- ResourceHub.tsx line 160: Print template footer "Created with LessonLens"
- ResourceHub.tsx line 376: Preview footer "LessonLens"
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide Logo Asset</name>
  <action>
    User provides the PiPi logo JPEG file. Claude will:
    1. Create the public/ directory
    2. Save the logo as public/logo.png (converting if needed)
    3. Create public/favicon.png (48x48 derived from logo)
  </action>
  <how-to-provide>
    Drag the PiPi logo JPEG file into this chat, or provide the file path.
  </how-to-provide>
  <resume-signal>Provide the logo file or file path</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update Browser Tab Branding</name>
  <files>index.html</files>
  <action>
    Update index.html with PiPi branding:

    1. Change title tag (line 7):
       FROM: `<title>LessonLens - AI Presentation Creator</title>`
       TO: `<title>PiPi</title>`

    2. Add favicon link inside <head>, after the title:
       `<link rel="icon" type="image/png" sizes="48x48" href="/PiPi/favicon.png">`

    IMPORTANT: Use `/PiPi/` prefix for favicon href (matches vite.config.ts base path).
  </action>
  <verify>
    Run `npm run dev` and check browser tab shows "PiPi" title and favicon.
  </verify>
  <done>Browser tab displays "PiPi" title and PiPi favicon</done>
</task>

<task type="auto">
  <name>Task 3: Update Header and ResourceHub Branding</name>
  <files>App.tsx, components/ResourceHub.tsx</files>
  <action>
    Update App.tsx header (lines 524-527):

    CURRENT:
    ```tsx
    <div className="flex items-center gap-3 cursor-pointer" onClick={() => setAppState(AppState.INPUT)}>
      <div className="w-8 h-8 bg-indigo-600 dark:bg-amber-500 rounded-lg flex items-center justify-center text-white dark:text-slate-900 font-fredoka font-bold text-lg shadow-md shadow-indigo-900/10 dark:shadow-amber-500/20">L</div>
      <h1 className="font-fredoka text-xl font-bold text-slate-800 dark:text-white tracking-tight">LessonLens</h1>
    </div>
    ```

    REPLACE WITH (logo only, no text, no click handler per CONTEXT.md):
    ```tsx
    <div className="flex items-center gap-3">
      <img
        src="/PiPi/logo.png"
        alt="PiPi"
        className="h-8 w-auto mix-blend-multiply dark:mix-blend-normal"
      />
    </div>
    ```

    NOTE: If dark mode looks wrong with mix-blend-mode, adjust className to:
    `className="h-8 w-auto"` (if logo was converted to transparent PNG)

    Update ResourceHub.tsx preview footer (line 376):
    FROM: `<span>LessonLens</span>`
    TO: `<span>PiPi</span>`

    Update ResourceHub.tsx print template footer (line 160):
    FROM: `<span>Created with LessonLens</span>`
    TO: `<span>Created with PiPi</span>`

    IMPORTANT: Use `/PiPi/` prefix for logo src (matches vite.config.ts base path).
  </action>
  <verify>
    1. Run `npm run dev`
    2. Verify header shows logo image (not "L" icon or "LessonLens" text)
    3. Create a test presentation, open ResourceHub
    4. Verify preview footer shows "PiPi"
    5. Click Print button, verify print preview shows "Created with PiPi"
    6. Toggle dark mode, verify logo displays correctly
  </verify>
  <done>
    - Header displays PiPi logo image only (no "L" icon, no "LessonLens" text)
    - ResourceHub preview footer shows "PiPi"
    - ResourceHub print output shows "Created with PiPi"
    - Logo displays correctly in both light and dark mode
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Branding</name>
  <what-built>Complete PiPi branding across all UI elements</what-built>
  <how-to-verify>
    1. Open http://localhost:3000/PiPi/ in browser
    2. Check browser tab shows "PiPi" title and favicon
    3. Check header displays PiPi logo (not "L" or "LessonLens")
    4. Toggle dark mode - verify logo looks correct
    5. Upload a PDF to create a presentation
    6. Click ResourceHub button
    7. Select any resource, check preview footer shows "PiPi"
    8. Click Print button, check print preview shows "Created with PiPi"
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
All branding requirements met:
- [x] BRND-01: Header displays PNG logo instead of "L" icon + "LessonLens" text
- [x] BRND-02: Browser tab title shows "PiPi"
- [x] BRND-03: ResourceHub footer/watermark shows "PiPi" instead of "LessonLens"

Additional checks:
- [ ] Favicon displays in browser tab
- [ ] Logo displays correctly in dark mode
- [ ] Print output has correct branding
</verification>

<success_criteria>
1. Browser tab shows "PiPi" title
2. Browser tab shows PiPi favicon
3. Header shows PiPi logo image only (no text)
4. ResourceHub preview footer shows "PiPi"
5. ResourceHub print output shows "Created with PiPi"
6. Logo displays correctly in both light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/07-branding/07-01-SUMMARY.md`
</output>
